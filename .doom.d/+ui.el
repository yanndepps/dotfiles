;;;  -*- lexical-binding: t; -*-

;; Fonts
(setq doom-font (font-spec :family "Hasklig" :size 12))
(setq doom-big-font (font-spec :family "Hasklig" :size 16))
;; Dash highlighting
;; (after! dash (dash-enable-font-lock))
